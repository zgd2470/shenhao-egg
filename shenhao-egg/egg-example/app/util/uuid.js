'use strict';
// eslint-disable-next-line no-undef
class UuidUtil {
    //获取uuid
    async getUuid() {
        // eslint-disable-next-line no-undef
        const uuid = require('node-uuid');
        return uuid;
    }
}

// eslint-disable-next-line no-undef
module.exports = UuidUtil;
